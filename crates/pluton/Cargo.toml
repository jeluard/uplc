[package]
name = "amaru-pluton"
version = { workspace = true }
edition = { workspace = true }

publish = false

[dependencies]
clap = { version = "4.5.18", features = ["derive"] }
miette = { version = "7.2.0", features = ["fancy"] }
amaru-uplc-turbo = { version = "0.1.1", path = "../uplc" }
